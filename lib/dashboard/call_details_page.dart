import 'package:flutter/material.dart';

class CallDetailsPage extends StatelessWidget {
  const CallDetailsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Call Details Page"),
      ),
    );
  }
}
